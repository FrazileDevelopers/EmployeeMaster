import 'package:flutter/material.dart';

var bluePinkGradient = const LinearGradient(
    colors: [Color(0xFFCC0099), Color(0xFF6600FF)],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [0.0, 1.0]);

var blueGreenGradient = const LinearGradient(
  colors: [Color(0xFF0099CC), Color(0xFF99FF66)],
  tileMode: TileMode.clamp,
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  stops: [0.0, 1.0],
);
var yellowOrangeGradient = const LinearGradient(
  colors: [Color(0xFFFFFF00), Color(0xFFFF6600)],
  tileMode: TileMode.clamp,
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  stops: [0.0, 1.0],
);
var pinkRedGradient = const LinearGradient(
    colors: [Color(0xFFFF3399), Color(0xFFFF0000)],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [0.0, 1.0]);

var blackBlueGradient = LinearGradient(
    colors: [Colors.blueGrey.shade800, Colors.black87],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: const [0.0, 1.0]);
var redSexyGradient = const LinearGradient(
    colors: [Color(0xFFF6356F), Color(0xFFFF5F50)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var greenSexyGradient = const LinearGradient(
    colors: [Color(0xFF92D000), Color(0xFFE1EB01)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var violetSexyGradient = const LinearGradient(
    colors: [Color(0xFF52A7EA), Color(0xFF712E98)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var blueSexyGradient = const LinearGradient(
    colors: [Color(0xFF46A3B7), Color(0xFF86F1DE)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var orangeSexyGradient = const LinearGradient(
    colors: [Color(0xFFFF9B30), Color(0xFFF8FF3F)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var thodaSexyGradient = const LinearGradient(
    colors: [Color(0xFFF58573), Color(0xFF5F79F4)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var skyBlueGradient = const LinearGradient(
    colors: [Color(0xFF15EDED), Color(0xFF029CF5)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
var blackSexyGradient = const LinearGradient(
    colors: [Color(0xFF90203F), Color(0xFF537895)],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [0.0, 1.0]);
